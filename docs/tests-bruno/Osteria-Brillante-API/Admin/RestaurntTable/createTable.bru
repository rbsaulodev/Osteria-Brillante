meta {
  name: createTable
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/tables
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "tableNumber": "02"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
