meta {
  name: createCategory
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "name": "Bebidas Geladas"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
